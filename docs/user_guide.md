# User Guide

## Quick Start

1. Install Python dependencies from `requirements.txt`.
2. Configure environment variables in `.env`.
3. Run visual tests via `src/scripts/run_visual_tests.sh`.
4. Generate reports via `src/scripts/generate_report.sh`.

This guide is a starter document and can be expanded with screenshots and troubleshooting.
